export type TMovie = {
  id: string;
  name: string;
  iconUri: string;
  manifestUri: string;
};

// There are coming objects with many more properties, but for purpose of this demo we ignore them...
export type TApiMovie = {
  name: string;
  iconUri: string;
  manifestUri: string;
};
